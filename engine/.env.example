# ═══════════════════════════════════════
# Alpha Trading Bot — Environment Variables
# Copy to .env and fill in your values
# ═══════════════════════════════════════

# Binance (required)
BINANCE_API_KEY=
BINANCE_SECRET=

# KuCoin (optional — enables arbitrage)
KUCOIN_API_KEY=
KUCOIN_SECRET=
KUCOIN_PASSPHRASE=

# Delta Exchange India (optional — enables futures)
DELTA_API_KEY=
DELTA_SECRET=
DELTA_TESTNET=true
DELTA_LEVERAGE=20
# Delta Exchange India linear perpetual format: ASSET/USD:USD
DELTA_PAIRS=BTC/USD:USD,ETH/USD:USD,SOL/USD:USD,XRP/USD:USD
ENABLE_SHORTING=true
# Delta Options overlay (buy CALLs/PUTs on 3/4+ scalp signals)
DELTA_OPTIONS_ENABLED=false
DELTA_OPTIONS_PAIRS=BTC/USD:USD,ETH/USD:USD
# Delta India fees (base rates, GST added automatically)
# Taker: 0.05% per side + 18% GST = 0.059% effective
# Maker: 0.02% per side + 18% GST = 0.024% effective
DELTA_TAKER_FEE=0.0005
DELTA_MAKER_FEE=0.0002
DELTA_GST_RATE=0.18

# Bybit (primary futures exchange — USDT-settled linear perpetuals)
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=false
BYBIT_LEVERAGE=20
BYBIT_PAIRS=BTC/USDT:USDT,ETH/USDT:USDT,SOL/USDT:USDT,XRP/USDT:USDT
# Bybit fees (no GST — global exchange)
# Taker: 0.055% per side, Maker: 0.02% per side
# Mixed round-trip (maker in + taker out) = 0.075%
BYBIT_TAKER_FEE=0.00055
BYBIT_MAKER_FEE=0.0002

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Trading — Binance spot pairs (1x, long-only, $6 min per trade)
TRADING_PAIRS=BTC/USDT,ETH/USDT,SOL/USDT,XRP/USDT
STARTING_CAPITAL=10.0
MAX_LOSS_DAILY_PCT=20.0
MAX_POSITION_PCT=80.0
# Binance spot: 0.1% per side, 0.2% round trip
# At 1.5% TP: net = 1.5% - 0.2% = 1.3% profit
# At 0.5% SL: net = 0.5% + 0.2% = 0.7% loss → R:R = 1.3/0.7 = ~1.9:1
